apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: postgres

helmCharts:
  - name: postgresql
    repo: https://registry-1.docker.io/bitnamicharts
    version: 16.6.6
    releaseName: postgresql-db
    namespace: postgres
    valuesFile: values.yaml

resources:
- ingress.yaml