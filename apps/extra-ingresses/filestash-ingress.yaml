apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: filestash-ingress
  namespace: filestash
spec:
  defaultBackend:
    service:
      name: filestash
      port:
        number: 80
  ingressClassName: tailscale
  tls:
    - hosts:
        - filestash